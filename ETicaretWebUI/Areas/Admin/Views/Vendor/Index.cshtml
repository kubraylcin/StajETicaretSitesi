@using ETicaretWebUI.Dtos.VendorDtos
@model List<VendorResultDto>
@{
    ViewData["Title"] = "Vendor Listesi";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
    int count = 0;
}
<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <h3 class="mb-3">Vendor Listesi</h3>

                <table class="table table-bordered table-striped">
                    <thead class="thead-dark">
                        <tr>
                            <th>#</th>
                            <th>Vendor Adı</th>
                            <th>Görsel</th>
                            <th>Sil</th>
                            <th>Güncelle</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>@count</td>
                                <td>@item.VendorName</td>
                                <td>
                                    @if (!string.IsNullOrEmpty(item.ImagePath))
                                    {
                                        <img src="@item.ImagePath" alt="Vendor Görseli" style="width: 80px; height: 100px; border-radius: 4px;" />
                                    }
                                    else
                                    {
                                        <span class="text-muted">Görsel yok</span>
                                    }
                                </td>
                                <td>
                                    <button class="btn btn-danger btn-delete" data-id="@item.VendorId">Sil</button>
                                </td>
                                <td>
                                    <a href="/Admin/Vendor/UpdateVendor/@item.VendorId" class="btn btn-success">Güncelle</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

                <div class="mt-3">
                    <a href="/Admin/Vendor/CreateVendor" class="btn btn-primary">Yeni Vendor Ekle</a>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const deleteButtons = document.querySelectorAll(".btn-delete");

            deleteButtons.forEach(button => {
                button.addEventListener("click", function () {
                    const vendorId = this.getAttribute("data-id");

                    Swal.fire({
                        title: 'Emin misin?',
                        text: "Bu işlemi geri alamazsın!",
                        icon: 'warning',
                        showCancelButton: true,
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        confirmButtonText: 'Evet, sil!',
                        cancelButtonText: 'Hayır, vazgeç'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            window.location.href = `/Admin/Vendor/DeleteVendor/${vendorId}`;
                        }
                    });
                });
            });
        });
    </script>
}
